/** Immutable version identifiers for clerk data provenance. */
export const ORACLE_PROFILE_VERSION = "tag-derived-v0";
export const RELATIONSHIP_GRAPH_VERSION = "unbuilt-v0";

/** Default pool coverage threshold — warn-only until evaluation supports hard block. */
export const DEFAULT_POOL_COVERAGE_THRESHOLD_PCT = 60;
